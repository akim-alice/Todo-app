import React from "react";

const Addoption = () => {
  const handleItem = () => {
  alert('als');
};
  return(
    <div>
      <button onClick={handleItem}>add-option</button>
    </div>
  
  );
};

export default Addoption;